import XCTest
@testable import GTFSPipline

final class GTFSPiplineTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GTFSPipline().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
